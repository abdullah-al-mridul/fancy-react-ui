import { BaseSpinnerProps } from "../types";
export interface GlitchSpinnerProps extends BaseSpinnerProps {
    /**
     * Text to display below the spinner
     * @default "Loading"
     */
    text?: string;
}
