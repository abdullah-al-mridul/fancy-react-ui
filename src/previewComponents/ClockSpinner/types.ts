import { BaseSpinnerProps } from "../types";

export interface ClockSpinnerProps extends BaseSpinnerProps {
  /**
   * Thickness of the clock hands and border in pixels
   * @default 2
   */
  thickness?: number;
}
